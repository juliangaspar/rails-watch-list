<h1>Watchlists</h1>
<div>
<% @lists.each do |list| %>
  <li><%= link_to list.name, list_path(list.id)%></li>
<% end %>
</div>
<div>
<% @movies.each do |movie| %>
<div><%= movie.title %></div>
<% end %>
</div>
